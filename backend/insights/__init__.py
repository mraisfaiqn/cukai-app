# AI Insights module — rule-engine + LLM-phrased insight feed for the
# InsightsInbox frontend. See engine.py for the TaxInsightEngine itself,
# router.py for the HTTP surface, and insight_models.py for the two tables
# (insights, insight_runs).