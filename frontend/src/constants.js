// Shared constants for the cukai.ai frontend.
//
// CURRENT_YEAR_OF_ASSESSMENT is used everywhere a page needs to fetch or
// display "this year's" Form B figures (Generate Report tab, Dashboard
// Overview, etc.) — keeping it in one place means those pages can never
// drift apart by querying different years.
export const CURRENT_YEAR_OF_ASSESSMENT = 2025;
